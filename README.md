# GitLab Token

You will need to create a file called '.env' and add your GitLab personal access token like:
```
TOKEN=token
```

# Packages needed:
- python-dotenv
- python-gitlab
- python-dateutil